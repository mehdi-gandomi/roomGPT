const config = {
    db: {
        /* don't expose password or any sensitive info, done only for demo */
        host: "localhost",
        user: "devbazar3d",
        password: "iRJixCDp5A5wXJw2",
        database: "devbazar3d",
        connectTimeout: 60000
    },
    listPerPage: 10,
};
module.exports = config;